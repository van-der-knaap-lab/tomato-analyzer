#include "stdafx.h"
#include <vector>
#include "../imgTool.h"

using namespace ipt;

std::vector<IPTPOINT>*  ellipse(int a, int b)
{
    int x, y, xchange, ychange, twoAsquare, twoBsquare, stopx, stopy;

    twoAsquare = 2*a*a;
    twoBsquare = 2*b*b;

    x = a;
    y = 0;
  //  xchange = b*b*();



    return 0;
}